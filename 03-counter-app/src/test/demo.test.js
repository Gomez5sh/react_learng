//Test agrupated

describe("Test in demo.text.js", () => {
  //test
  test("String are same", () => {
    // 1. Init
    const mens = "Hello World!";

    // 2. second
    const mens2 = "Hello World!";

    // 3. See the Output
    expect(mens).toBe(mens2);
  });
});
